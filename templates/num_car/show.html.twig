{% extends 'base.html.twig' %}

{% block title %}NumCar{% endblock %}

{% block body %}
    <h1>NumCar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ num_car.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ num_car.number }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ num_car.region }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('num_car_index') }}">back to list</a>

    <a href="{{ path('num_car_edit', {'id': num_car.id}) }}">edit</a>

    {{ include('num_car/_delete_form.html.twig') }}
{% endblock %}
